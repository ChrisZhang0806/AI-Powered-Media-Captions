# Optional backend default. Users can also provide their own API key in the UI.
# Never expose this value through a VITE_ environment variable.
OPENAI_API_KEY=
PORT=3001
UPLOAD_DIR=
OUTPUT_DIR=
UPLOAD_CHUNK_SIZE_MB=8
MEDIA_PROCESSING_CONCURRENCY=2
STREAMING_TRANSCRIPTION_CONCURRENCY=2
STREAMING_SEGMENT_SECONDS=35
STREAMING_SEGMENT_MAX_SECONDS=50
STREAMING_SILENCE_THRESHOLD=160
